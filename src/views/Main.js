import React, {useState} from 'react';

function Main() {
    // Declare a new state variable, which we'll call "count"  const [count,
    // setCount] = useState(0);
    return (
        <div>
            <p>Main</p>
        </div>
    );
}
export default Main;